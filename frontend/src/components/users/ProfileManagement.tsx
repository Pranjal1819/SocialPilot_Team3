"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { useCurrentUser } from "@/hooks/useCurrentUser";
import { ROLE_LABELS } from "@/types/user";

function Preference({ title, description, enabled, onChange }: { title: string; description: string; enabled: boolean; onChange: () => void }) {
  return <div className="flex items-center justify-between gap-5 py-5"><div><p className="text-sm font-semibold text-slate-800">{title}</p><p className="mt-1 text-sm text-slate-500">{description}</p></div><button aria-pressed={enabled} onClick={onChange} className={`flex h-7 w-12 shrink-0 items-center rounded-full p-0.5 transition ${enabled ? "justify-end bg-sky-600" : "justify-start bg-slate-200"}`}><span className="h-6 w-6 rounded-full bg-white shadow-sm" /></button></div>;
}

export function ProfileManagement() {
  const { name, email, role } = useCurrentUser();
  const [profile, setProfile] = useState({ name, email, organization: "SocialPilot", bio: "Building better social media workflows.", phone: "+91 98765 43210" });
  const [notifications, setNotifications] = useState(true);
  const [darkMode, setDarkMode] = useState(false);
  const [language, setLanguage] = useState("English");
  return <div className="grid gap-6 lg:grid-cols-[minmax(0,1.4fr)_minmax(300px,0.8fr)]"><section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm sm:p-7"><div className="flex items-center gap-4 border-b border-slate-100 pb-6"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-sky-700 text-xl font-bold text-white">{name.charAt(0)}</div><div><h2 className="text-lg font-semibold text-slate-900">{name}</h2><p className="text-sm text-slate-500">{ROLE_LABELS[role]} · {profile.organization}</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Full name<Input className="mt-2" value={profile.name} onChange={(event) => setProfile({ ...profile, name: event.target.value })} /></label><label className="text-sm font-medium text-slate-700">Email address<Input className="mt-2" type="email" value={profile.email} onChange={(event) => setProfile({ ...profile, email: event.target.value })} /></label><label className="text-sm font-medium text-slate-700">Organization<Input className="mt-2" value={profile.organization} onChange={(event) => setProfile({ ...profile, organization: event.target.value })} /></label><label className="text-sm font-medium text-slate-700">Contact number<Input className="mt-2" value={profile.phone} onChange={(event) => setProfile({ ...profile, phone: event.target.value })} /></label><label className="text-sm font-medium text-slate-700 sm:col-span-2">Bio<textarea className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 bg-slate-50 p-3.5 text-sm outline-none transition focus:border-sky-400 focus:bg-white focus:ring-4 focus:ring-sky-50" value={profile.bio} onChange={(event) => setProfile({ ...profile, bio: event.target.value })} /></label></div><Button className="mt-6">Save profile</Button></section><aside className="space-y-6"><section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Preferences</h2><div className="mt-2 divide-y divide-slate-100"><Preference title="Email notifications" description="Updates about scheduled posts and team activity." enabled={notifications} onChange={() => setNotifications(!notifications)} /><Preference title="Theme preference" description={darkMode ? "Dark theme selected." : "Light theme selected."} enabled={darkMode} onChange={() => setDarkMode(!darkMode)} /></div><label className="mt-4 block text-sm font-medium text-slate-700">Language<select value={language} onChange={(event) => setLanguage(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3.5 text-sm outline-none focus:border-sky-400"><option>English</option><option>Hindi</option></select></label></section><section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm"><h2 className="text-base font-semibold text-slate-900">Security</h2><p className="mt-1 text-sm leading-6 text-slate-500">Password, active sessions, and account deletion will connect to the authentication API when it is available.</p><div className="mt-4 flex flex-wrap gap-2"><Button variant="outline" size="sm">Manage sessions</Button><Button variant="outline" size="sm" className="border-red-200 text-red-600 hover:bg-red-50">Delete account</Button></div></section></aside></div>;
}
